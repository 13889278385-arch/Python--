有无 f 的核心区别
name = "小明"
score = 88.72
print(f"{name}的成绩是 {score:.1f} 分")
# 输出：小明的成绩是 88.7 分



print("{name}的成绩是 {score:.1f} 分")
# 原样输出：{name}的成绩是 {score:.1f} 分


不带 f，只能这么写（很麻烦）
print(name + "成绩" + str(score))
