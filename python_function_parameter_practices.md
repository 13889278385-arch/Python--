# Python 函数参数使用规范

## 一、不要使用可变对象作为默认参数

### 错误写法：默认参数使用空列表

```python
def func(a=[]):
    a.append(1)
    print(a)

func()  # [1]
func()  # [1, 1]
func()  # [1, 1, 1]
```

这种结果看起来很诡异：每次调用函数时，列表都在累加，而不是重新得到一个空列表。

### 底层原因

函数定义时，默认参数只会创建一次，并保存在函数对象中。多次调用函数时，会重复使用同一个列表对象。

由于列表是可变对象，前一次调用对列表的修改会被保留下来，后续调用就会看到之前的修改结果。

可以用 `id()` 观察对象是否相同：

```python
def func(a=[]):
    print(id(a))

func()
func()
# 两次输出的 id 相同
```

## 二、标准写法：使用 `None` 占位

将默认值设置为不可变的 `None`，真正需要列表时，在函数内部创建新的列表：

```python
def func(a=None):
    # 如果没有传入参数，就新建空列表
    if a is None:
        a = []

    a.append(1)
    print(a)

func()  # [1]
func()  # [1]
```

每次调用时，`a is None` 都会成立，于是函数内部都会创建一个全新的空列表。

### 字典同理

```python
def func(d=None):
    if d is None:
        d = {}

    d["count"] = d.get("count", 0) + 1
    print(d)

func()  # {'count': 1}
func()  # {'count': 1}
```

### 常见可变对象

以下对象不建议直接作为默认参数：

- 列表：`[]`
- 字典：`{}`
- 集合：`set()`
- 其他会被修改的自定义对象

推荐统一使用 `None` 占位，再在函数内部创建对象。

## 三、少用全局变量，多用“参数传入 + `return` 返回”

### 反面写法：使用全局变量

```python
# 全局变量：整个文件中的代码都可能修改它
num = 10

def add():
    global num
    num += 5

add()
print(num)  # 15
```

这种写法的问题是：

- 变量可以被多个函数或代码位置修改；
- 代码规模变大后，很难追踪到底是谁改变了它；
- 函数依赖外部状态，不容易单独测试；
- 容易产生难以定位的 bug。

## 四、规范写法：参数传入，返回结果

```python
def add(x):
    return x + 5

res = add(10)
print(res)  # 15
```

这个函数的数据流很清晰：

```text
传入 x → 函数计算 → return 返回结果
```

函数不依赖外部全局变量，也不直接修改外部数据，因此更独立、更容易调试和测试。

## 五、最终记忆口诀

> 默认参数不要放可变对象，使用 `None` 占位，函数内部再创建。  
> 少改全局变量，多用参数传入和 `return` 返回。  
> 让函数的数据流清晰，尽量减少隐藏的副作用。
